/**
 * Un programa que acepta 5 argumentos enteros
 * Imprime la suma de los numeros impares*/
 #include <stdio.h>
 #include <stdlib.h>

int main(int argc, char *argv[]) {
  int a = atoi(argv[1]);
  int b = atoi(argv[2]);
  int c = atoi(argv[3]);
  int d = atoi(argv[4]);
  int e = atoi(argv[5]);
  int p = (a + b);
  int q = (b + c);
  int r = (c + d);
  int x = (d + e);
  printf("%d\n",a,b);



  return 0;

}
